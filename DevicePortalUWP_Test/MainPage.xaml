<Page
    x:Class="DevicePortalUWP_Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevicePortalUWP_Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Hub Header="DevicePortalUWP">
            <HubSection Header="" Height="630" Width="360">
                <DataTemplate>
                    <Grid/>
                </DataTemplate>
            </HubSection>
        </Hub>
        <RelativePanel>
            <Button Content="?" Click="Button_Click" Margin="317,22,15,0" VerticalAlignment="Top" HorizontalAlignment="Right" HorizontalContentAlignment="Right" RelativePanel.AlignRightWith="" RelativePanel.AlignRightWithPanel="True" />

        </RelativePanel>
        <WebView Margin="0,68,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Source="http://127.0.0.1"/>
    </Grid>
</Page>
